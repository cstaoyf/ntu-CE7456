
\include{./def/yf-formatting}
\include{./def/yf-def}

\usepackage{wasysym}
%\usepackage{times}

\title{}
\author{}
\date{}


%\def\A{\mathcal{A}}
%\def\B{\mathcal{B}}
\def\C{\mathcal{C}}
%\def\D{\mathcal{D}}
\def\E{\mathcal{E}}
\def\FF{\EuScript{F}}
\def\G{\mathcal{G}}
\def\GG{\EuScript{G}}
\def\H{\mathcal{H}}
\def\I{\mathcal{I}}
\def\J{\mathcal{J}}
\def\L{\mathcal{L}}
\def\Q{\mathcal{Q}}
\def\S{\mathcal{S}}
\def\T{\mathcal{T}}
\def\V{\mathcal{V}}
%\def\W{\EuScript{W}}
\def\W{\mathscr{W}}
\def\X{\mathcal{X}}
\def\Y{\mathcal{Y}}
\def\Z{\mathcal{Z}}
\def\att{\mit{diffatt}}
\def\boldatt{\mathbf{att}}
\def\ext{\mathit{ext}}
\def\join{\mathit{join}}
\def\schema{\mathit{schema}}


\def\extraspacing{\vspace{4mm} \noindent}
\def\vgap{\vspace{4mm}}
\def\vslit{\vspace{2mm}}

\begin{document}

\boxminipg{\linewidth}{
    \begin{center}
        \vspace{3mm}
    {\Large
    (CE7456 Lecture Notes 10) Natural Joins} \\[2mm]
    {\large Yufei Tao}
    \vspace{3mm}
    \end{center}
}

Today, we discuss the {\em natural join} problem, which lies at the core of database theory and generalizes the subgraph enumeration problem in the previous lecture.

\vgap

Let $\boldatt$ be a finite set, where each element is called an {\em attribute}. For a non-empty set $\X \subseteq \boldatt$ of attributes, a {\em tuple} over $\X$ is a function $\bm{u}: \X \rightarrow \intdom$ (where $\intdom$ represents the set of integers). For any non-empty subset $\Y \subseteq \X$, we define $\bm{u}[\Y]$ --- called the {\em projection} of $\bm{u}$ on $\Y$ --- as the tuple $\bm{v}$ over $\Y$ satisfying $\bm{v}(Y) = \bm{u}(Y)$ for every attribute $Y \in \Y$. A {\em relation} $R$ is a set of tuples over the same set of attributes; we refer to that set as the {\em schema} of $R$ and represent it as $\schema(R)$.

\vgap

We define a {\em join} as a set $\Q$ of relations. Let $\schema(\Q) = \bigcup_{R \in Q} \schema(R)$. The result of $\Q$ is a relation over $\schema(\Q)$ formalized as:
\myeqn{
    \join(\Q) &=& \{\textrm{tuple $\bm{u}$ over $\schema(\Q)$} \mid \forall R \in \Q: \bm{u}[\schema(R)] \in R\}. \label{eqn:join-rslt}
}
Our discussion in this lecture assumes that $\schema(\Q)$ has a constant size.

\extraspacing {\bf Math Conventions.} We define $0^0 = 0$. For an integer $x \ge 1$, the notation $[x]$ denotes the set $\{1, 2, ..., x\}$. Every logarithm $\log(\cdot)$ has base $2$.

%The notation $\tO(.)$ hides a factor logarithmic to $\sum_{R \in \Q} |R|$.

\section{Schema Graphs and Fractional Edge Covers} \label{sec:schema}

Given a join $\Q$, we define its {\em schema graph} as the hypergraph $\H = (\V, \E)$ where
\myeqn{
    \V &=& \schema(\Q) \nn \\
    \E &=& \set{\schema(R) \mid R \in \Q}. \nn
}
Note that there is one-one correspondence between the relations in $\Q$ and the hyperedges in $\E$. For each hyperedge $e \in \E$, let $R_e$ represent the corresponding relation $R \in \Q$.

\vgap

Regard $\E$ as a ``space'', next we define vectors over it. Every such vector $\W$ has a real-valued component $\W_e$ on each hyperedge $e \in \E$. The vector $\W$ is said to be a {\em fractional edge cover} of $\H$ if it satisfies two conditions:
\myitems{
    \item $0 \le \W_e \le 1$ for every $e \in \E$.
    \item $\sum_{e \in \E : X \in e} \W_e \ge 1$ for every $X \in \V$.
}

\section{AGM Bound} \label{sec:agm}

Let $\Q$ be a join with schema graph $\H = (\V, \E)$. Every fractional edge cover $\W$ of $\H$ is associated with an {\em AGM value} defined as
\myeqn{
    \mit{AGM}_\W &=& \prod_{e \in \E} |R_e|^{\W_e}. \label{eqn:agm}
}

\vgap

\begin{theorem} \label{thm:agm}
   For any fractional edge cover $\W$ of $\H$., it holds that $|\join(\Q)| \le \mit{AGM}_\W$.
\end{theorem}

\vgap

An {\em optimal fractional edge cover} of $\H$ is a fractional edge cover $\W^*$ minimizing \eqref{eqn:agm}. The {\em AGM bound} of $\Q$ is defined as the AGM value of $\W^*$.

\vgap

The AGM bound is tight:

\vgap

\begin{theorem} \label{thm:agm-tight}
   Fix an arbitrary hypergraph $\H = (\V, \E)$ and an arbitrary integer vector $N$ over $\E$ such that $N_e \ge 1$ for each $e \in \E$. There exists a join $\Q$ satisfying
   \myitems{
        \item the schema graph of $\Q$ is $\H$;
        \item for each $e \in \E$, $|R_e| = N_e$;
        \item $|\join(\Q)|$ is at least the AGM bound of $\Q$ up to a constant factor.
   }
\end{theorem}

\vgap

We prove Theorem~\ref{thm:agm} in the next section and Theorem~\ref{thm:agm-tight} in the next lecture.

\section{Proof of Theorem~\ref{thm:agm}} \label{sec:agm-proof}

Given a tuple $\bm{u}$ over a subset $U \subseteq \E$ (recall that $\E = \schema(\Q)$) and a relation $R \in \Q$, we define a {\em residual relation} as:
\myeqn{
    R(\bm{u}) &=&
    \set{
        \bm{v} \in R \mid \forall X \in U \cap \schema(R): \bm{v}(X) = \bm{u}(X)
    }. \label{eqn:alg:semi-join}
}
Intuitively, $R(\bm{u})$ includes those tuples of $R$ that are ``constrained'' by $\bm{u}$, in the sense that they must take the same value as $\bm{u}$ on every attribute in $U$. Note that if $U$ and $\schema(R)$ are disjoint, then $R(\bm{u}) = R$. In relational algebra, \eqref{eqn:alg:semi-join} is referred to as the {\em semi-join} of $R$ with $\bm{u}$.

\vgap

Given a tuple $\bm{u}$ over a subset $U \subseteq \E$, we define a {\em residual join} as follows:
\myeqn{
    \Q(\bm{u}) &=&
    \set{
        R_e(\bm{u}) \mid e \in \E
    }. \label{eqn:alg:residual}
}

\vslit

\begin{lemma} \label{lmm:agm-proof:residual}
    Fix an arbitrary order of the attributes in $\V$: $X_1, X_2, ..., X_d$ where $d = |\V|$. Let $t \in [0, d - 1]$ be an integer, and $\bm{u}$ a tuple over $U = \set{X_1, ..., X_t}$. We have
    \myeqn{
        |\join(\Q(\bm{u}))|
        &\le&
        \prod_{e \in \E} |R_e(\bm{u})|^{\W_e}. \label{eqn:agm-proof:residual-goal}
    }
\end{lemma}

\vgap

Specially, if $t = 1$, then $U = \emptyset$ and $\Q = \Q(\bm{u})$ (because $R(\bm{u}) = R$ for all $R \in \Q$). Hence, Theorem~\ref{thm:agm} is a corollary of the above lemma with $U = \emptyset$.

\extraspacing {\bf Proof of Lemma~\ref{lmm:agm-proof:residual}.} In general, given a relation $R$ and an attribute $X$ in its schema, we define
\myeqn{
    \pi_X (R) &=& \set{\bm{v}(X) \mid \bm{v} \in R}. \label{eqn:agm-proof:proj}
}
This is the {\em projection} operator in relational algebra.

\vgap

We prove the lemma by induction. As the base case, consider $t = d-1$. Because $\W$ is a fractional edge cover of $\H = (\V, \E)$, it holds that
\myeqn{
    \sum_{e \in \E : X_d \in e} \W_e &\ge& 1.
    \label{eqn:agm-proof:residual-goal:1}
}
If a relation $R \in \Q$ does not include $X_d$ in its schema, the residual relation $R(\bm{u})$ has at most one tuple. Otherwise, the tuples of $R(\bm{u})$ differ only in their values on $X_d$. If $\bm{v}$ is a tuple in $\join(\Q(\bm{u}))$,  we must have
\myeqn{
    \bm{v}(X_d) &\in&
        \bigcap_{e \in \E: X_d \in e} \pi_{X_d}(R_e(\bm{u})). \label{eqn:agm-proof:residual-goal:2}
}
Hence:
\myeqn{
    |\join(\Q(\bm{u}))|
    &\le&
    \min_{e \in \E : X_d \in e} |R_e(\bm{u})| \nn \\
    \explain{by \eqref{eqn:agm-proof:residual-goal:1}}
    &\le&
    \Big(
        \min_{e \in \E : X_d \in e} |R_e(\bm{u})|
    \Big)^{\sum_{e \in \E : X_d \in e} \W_e} \nn \\
    &=&
    \prod_{e \in \E : X_d \in e}
    \Big(
        \min_{e \in \E : X_d \in e} |R_e(\bm{u})|
    \Big)^{\W_e} \nn \\
    &\le&
    \prod_{e \in \E : X_d \in e} |R_e(\bm{u})|^{\W_e}. \nn
}

\vslit

Inductively, assuming that the lemma holds for $t = i$ (where $i \in [d]$), next we prove its correctness for $t = i - 1$. Because $\W$ is a fractional edge cover of $\H = (\V, \E)$, it holds that
\myeqn{
    \sum_{e \in \E: X_i \in e} &\ge& 1.
    \label{eqn:agm-proof:residual-goal:3}
}
Define:
\myeqn{
    \I = \bigcap_{e \in \E : X_i \in e} \pi_{X_i}(R_e(\bm{u})). \label{eqn:agm-proof:intersection}
}
For each value $x \in \I$, we construct a tuple $\bm{v}$ over $\set{X_1, ..., X_i}$ by ``extending $\bm{u}$ with $X_i = x$'', i.e., $\bm{v}(X_j) = \bm{u}(X_j)$ for all $j \in [i - 1]$ and $\bm{v}(X_i) = x$. This produces a set of ``extended'' tuples:
\myeqn{
    S_\ext &=&
    \set{
        \text{tuple $\bm{v}$ extended from $\bm{u}$ and $x$} \mid
        \text{$x \in \I$}
    }. \label{eqn:agm-proof:S_ext}
}
It is clear that
\myeqn{
    \join(\bm{u})
    &=&
    \bigcup_{\bm{v} \in S_\ext} \join(\bm{v}). \nn
}
With the above, we derive
\myeqn{
    |\join(\bm{u})|
    &=&
    \sum_{\bm{v} \in S_\ext} |\join(\bm{v})| \nn \\
    \explain{by inductive assumption}
    &\le&
    \sum_{\bm{v} \in S_\ext}
    \prod_{e \in \E}
    |R_e(\bm{v})|^{\W_e} \nn \\
    &\le&
    \sum_{\bm{v} \in S_\ext}
    \Big(
    \prod_{e \in \E : X_i \in e}
    |R_e(\bm{v})|^{\W_e}
    \Big)
    \Big(
    \prod_{e \in \E : X_i \notin e}
    |R_e(\bm{v})|^{\W_e}
    \Big)
    \label{eqn:agm-proof:1}
}
If an edge $e \in \E$ does not contain $X_i$, we have $R_e(\bm{v}) = R_e(\bm{u})$. Therefore:
\myeqn{
    \eqref{eqn:agm-proof:1}
    &=&
    \sum_{\bm{v} \in S_\ext}
    \Big(
    \prod_{e \in \E : X_i \in e}
    |R_e(\bm{v})|^{\W_e}
    \Big)
    \Big(
    \prod_{e \in \E : X_i \notin e}
    |R_e(\bm{u})|^{\W_e}
    \Big) \nn \\
    &=&
    \Big(
    \prod_{e \in \E : X_i \notin e}
    |R_e(\bm{u})|^{\W_e}
    \Big)
    \sum_{\bm{v} \in S_\ext}
    \Big(
    \prod_{e \in \E : X_i \in e}
    |R_e(\bm{v})|^{\W_e}
    \Big) \nn \\
    &\le&
    \Big(
    \prod_{e \in \E : X_i \notin e}
    |R_e(\bm{u})|^{\W_e}
    \Big)
    \prod_{e \in \E: X_i \in e}
    \Big(
        \sum_{\bm{v} \in S_\ext} |R_e(\bm{v})|
    \Big)^{\W_e} \nn \\
    &&
    \explain{by \eqref{eqn:agm-proof:residual-goal:3} and Friedgut's inequality in Appendix \ref{app:friedgut}}
    \nn \\[2mm]
    &\le&
    \Big(
    \prod_{e \in \E : X_i \notin e}
    |R_e(\bm{u})|^{\W_e}
    \Big)
    \prod_{e \in \E: X_i \in e}
        |R_e(\bm{u})|^{\W_e} \nn \\
    &=&
    \prod_{e \in \E}
    |R_e(\bm{u})|^{\W_e}. \nn
}
This completes the inductive step.

%
% \section{A Join Algorithm} \label{sec:alg}
%
% Set $d = |\V|$ and fix an arbitrary order of the attributes in $\V$: $X_1, X_2, ..., X_d$. To generate a tuple in $\join(\Q)$, we need to decide its value on each attribute. The idea of the algorithm is to finalize one value at a time.
%
% \vgap
%
% As the base case, suppose that we have finalized the values on $X_1, ..., X_{d-1}$. In other words, we have obtained a tuple $\bm{u}$ over $U = \set{X_1, ..., X_{d-1}}$ such that $|R(\bm{u})| \ge 1$ for all $R \in \Q$. It remains to find all tuples $\bm{v} \in \join(\Q)$ that agree with $\bm{u}$ on all of $X_1, ..., X_{d-1}$, that is, $\bm{v}(X_i) = \bm{u}(X_i)$ for all $i \in [d-1]$ --- those tuples constitute exactly the result of the residual join $\Q(\bm{u})$. Computing all such $\bm{v}$ is equivalent to finding a set intersection:
% \myeqn{
%     \bigcap_{e \in \E: X_d \in e} \pi_{X_d}(R_e(\bm{u})). \nn
% }
%
% \vslit
%
% Inductively, suppose that we know how to compute the result of $\Q(\bm{u})$ for any tuple $\bm{u}$ over $\set{X_1, ..., X_t}$ where $t$ is an integer satisfying $t \ge 1$. Next, we show how compute the result of $\Q(\bm{v})$ for any tuple $\bm{v}$ over $\set{X_1, ..., X_{t-1}}$. Note that if $t = 1$, then $\bm{v}$ is an empty tuple, in which case $\Q(\bm{v}) = \Q$.
%
% \vgap
%
% First, we compute
% \myeqn{
%     \bigcap_{e \in \E(X_t)} \pi_{X_t}(R_e \ltimes \bm{v}). \label{eqn:alg:intersection}
% }
% For each value $x$ in the intersection of \eqref{eqn:alg:intersection}, we construct a tuple over $\set{X_1, ..., X_t}$ by ``extending $\bm{v}$ with $X_t = v$'', i.e., $\bm{u}(X_i) = \bm{v}(X_i)$ for all $i \in [t-1]$ and $\bm{u}(X_t) = x$. This results in a set of ``extended'' tuples defined as:
% \myeqn{
%     S_\ext &=&
%     \set{
%         \text{tuple $\bm{u}$ extended from $x$} \mid
%         \text{$x$ in the intersection in \eqref{eqn:alg:intersection}}
%     }. \nn
% }
% For each such tuple $\bm{u}$, we compute the result of $\Q(\bm{u})$. The result of $\Q(\bm{v})$ is the union of the $\join(\Q(\bm{u}))$ of all $\bm{u} \in S_\ext$.
%
% \section{Analysis}
%
% \extraspacing {\bf Analysis.} Let us start with a technical lemma:
%
% \begin{lemma} \label{lmm:alg:decomp}
%    Let $\W$ be an arbitrary fractional edge cover of $\H$, and $S$ be the set of values in the intersection computed at \eqref{eqn:alg:intersection}. We have:
%    \myeqn{
%         \sum_{\bm{u} \in S_\ext} \prod_{e \in \E(X_j)} |R_e(\bm{u})|^{\W_e} &\le&  \nn
%    }
% \end{lemma}
%
%
%
%
%
% We make the following assumptions in our analysis:
% \myitems{
%     \item Given a tuple $\bm{u}$ over a set $U \subseteq \schema(\Q)$ and a relation $R \in \Q$, we can compute $R(\bm{u})$ --- defined in \eqref{eqn:alg:semi-join} --- in $O(1 + |R(\bm{u})|)$ time and $|R(\bm{u})|$ in $O(1)$ time.
%
%     \item Given a relation $R \in \Q$ and a tuple $\bm{u}$ over $\schema(R)$, we can decide whether $\bm{u} \in R$ in $O(1)$ time.
% }
% These assumptions can be removed by slowing down the running time we will prove by a logarithmic factor (details left to you).
%
% \begin{lemma}
%     The intersection in \eqref{eqn:alg:intersection} can be computed in $O(\min_{e \in \E(X_t)} |R_e \ltimes \bm{v}|)$ time.
% \end{lemma}
% \begin{proof}
%    Define $e^* = \argmin_{e \in \E(X_t)} |R_e \ltimes \bm{v}|$. First, extract $R_{e^*} \ltimes \bm{v}$. For each tuple $\bm{u} \in R_{e^*} \ltimes \bm{v}$, check whether $\bm{u} \in R_e$ for every other $e \in \E(X_t)$. As $\E(X_t)$ has a constant size, the total running time is $O(1 + |R_{e^*} \ltimes \bm{v}|)$.
% \end{proof}

\bibliographystyle{plainurl}% the mandatory bibstyle
\bibliography{ref}

\appendix

\section{Appendix: Friedgut's Inequality} \label{app:friedgut}

Fix arbitrary integers $p$ and $q$ at least 1. Take a set of non-negative real values $\{a_{i,j} \mid i \in [p], j \in [q]\}$. In addition, take another set of non-negative real values $\{b_i$ $\mid$ $i \in [q]\}$ satisfying $\sum_{i=1}^{q} b_i \geq 1$.  Friedgut's inequality \cite{f04} states that
    \myeqn{
        \sum_{i=1}^{p}	\prod_{j=1}^{q} a_{i, j}^{b_j}
        &\leq&
        \prod_{j=1}^{q} \left(\sum_{i=1}^{p} a_{i, j}\right)^{b_j}.
        \label{eqn:friedgut}
    }

\end{document}
